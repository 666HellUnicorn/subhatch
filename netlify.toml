[build]
  command = "echo 'No build step'"
  publish = "."

[functions]
  node_bundler = "esbuild"
  included_files = ["src/**", "api/**"]

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
