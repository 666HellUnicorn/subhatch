[build]
  command = "echo 'No build step'"
  publish = "."

[functions]
  node_bundler = "esbuild"
  included_files = ["src/**", "adapters/**"]

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
